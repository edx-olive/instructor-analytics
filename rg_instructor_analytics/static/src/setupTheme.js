/**
 * Default MUI theme customization.
 * Refer to https://material-ui.com/styles/advanced/
 */

import { createMuiTheme } from "@material-ui/core";

export default createMuiTheme({
  customPalette: {
    greeny: "#91CF96"
  },
  palette: {
    primary: {
      main: "#00a2da"
    },
    secondary: {
      main: "#f6706f"
    }
  }
});
